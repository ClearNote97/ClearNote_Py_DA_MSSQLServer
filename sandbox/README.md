# `sandbox/` — experimentos desechables

Aquí se tantea sin miedo: probar una conexión, inspeccionar un dataframe, romper cosas.

- **El contenido NO se versiona** (`.gitignore` ignora todo salvo este `README.md`).
- Es el primer eslabón del flujo **`sandbox/` → `tests/` → `output/`**: nada llega al entregable
  sin pasar antes por el gate de `tests/`.
