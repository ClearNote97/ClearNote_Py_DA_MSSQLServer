# `notebooks/` — exploración y prototipado

Notebooks (`.ipynb` / `.py`) para explorar datos, prototipar y comunicar hallazgos de forma iterativa.

- Es espacio de **exploración**, no de entregable: lo que se consolide pasa a `src/` (código reutilizable)
  o al flujo `sandbox/ → tests/ → output/`.
